<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 边框 -->
    <stroke
        android:width="1.5dp"
        android:color="@android:color/darker_gray"/>

    <!-- 边距 -->
    <padding
        android:bottom="@dimen/text_mergin"
        android:left="@dimen/text_mergin"
        android:right="@dimen/text_mergin"
        android:top="@dimen/text_mergin" />

    <!-- 圆角 -->
    <corners android:radius="@dimen/radius" />

</shape>