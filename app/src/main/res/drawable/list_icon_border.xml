<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:layerType="software">

    <!-- 边框 -->
    <stroke
        android:width="@dimen/text_border_width"
        android:color="@android:color/darker_gray"/>

    <!-- 边距 -->
    <padding
        android:bottom="@dimen/list_border"
        android:left="@dimen/list_border"
        android:right="@dimen/list_border"
        android:top="@dimen/list_border" />

</shape>