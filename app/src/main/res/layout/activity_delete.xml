<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/delete_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:background="@color/select_photo_background">

        <Button
            android:id="@+id/delete"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/select_photo_btn_margin"
            android:layout_marginRight="@dimen/select_photo_btn_margin"
            android:layout_marginTop="20dp"
            android:padding="10dp"
            android:text="@string/delete"
            android:background="@drawable/btn_selector"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:onClick="deleteListener"/>

        <Button
            android:id="@+id/cancel"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/select_photo_btn_margin"
            android:layout_marginRight="@dimen/select_photo_btn_margin"
            android:layout_marginBottom="20dp"
            android:layout_marginTop="5dp"
            android:padding="10dp"
            android:text="@string/cancel"
            android:background="@drawable/btn_selector"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:onClick="cancelListener"/>

    </LinearLayout>
</RelativeLayout>
